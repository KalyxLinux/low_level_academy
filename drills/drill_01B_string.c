// Declare a string and print it with %s
#include <stdio.h>

int main()
{
    // Declare a string
    char name[32] = "Kalyx";

    // Print the string
    printf("name: %s\n", name);

    return 0;
};